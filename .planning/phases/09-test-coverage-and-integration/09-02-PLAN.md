---
phase: 09-test-coverage-and-integration
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - packages/core/parser.test.ts
  - packages/core/validator.test.ts
  - packages/core/expression.test.ts
  - packages/core/dag.test.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04

must_haves:
  truths:
    - "parser.ts has 90%+ line and branch coverage"
    - "validator.ts has 90%+ line and branch coverage"
    - "expression.ts has 90%+ line and branch coverage"
    - "dag.ts has 90%+ line and branch coverage"
  artifacts:
    - path: "packages/core/parser.test.ts"
      provides: "Parser tests covering identified gaps"
    - path: "packages/core/validator.test.ts"
      provides: "Validator tests covering identified gaps"
    - path: "packages/core/expression.test.ts"
      provides: "Expression engine tests covering identified gaps"
    - path: "packages/core/dag.test.ts"
      provides: "DAG resolver tests covering identified gaps"
  key_links:
    - from: "packages/core/parser.test.ts"
      to: "packages/core/parser.ts"
      via: "import { parse }"
      pattern: "import.*parse"
    - from: "packages/core/validator.test.ts"
      to: "packages/core/validator.ts"
      via: "import { validate }"
      pattern: "import.*validate"
---

<objective>
Fill coverage gaps identified in COVERAGE-BASELINE.md to bring all four core modules (parser, validator, expression, dag) to 90%+ coverage. Only write tests for uncovered lines/branches -- do not duplicate existing tests.

Purpose: Achieve the 90% coverage target on all four modules with minimal, targeted tests.
Output: Updated test files with gap-closing tests, all passing, 90%+ coverage confirmed.
</objective>

<execution_context>
@/Users/rainierpotgieter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainierpotgieter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-test-coverage-and-integration/09-01-SUMMARY.md
@.planning/phases/09-test-coverage-and-integration/COVERAGE-BASELINE.md
@packages/core/parser.ts
@packages/core/validator.ts
@packages/core/expression.ts
@packages/core/dag.ts
@packages/core/parser.test.ts
@packages/core/validator.test.ts
@packages/core/expression.test.ts
@packages/core/dag.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add targeted tests to close coverage gaps</name>
  <files>packages/core/parser.test.ts, packages/core/validator.test.ts, packages/core/expression.test.ts, packages/core/dag.test.ts</files>
  <action>
    1. Read COVERAGE-BASELINE.md to identify exactly which lines/branches are uncovered per module.
    2. Read each existing test file to understand what is already tested (avoid duplication).
    3. For each module below 90%, add a new describe block (e.g., "coverage gap tests") with targeted tests:

    **parser.ts potential gaps:**
    - YAML parse error catch branch (line 82-96): Test with malformed YAML that throws (e.g., `---\n: invalid: :\n---`)
    - Error with missing `.reason` but present `.message` (line 88 fallback)
    - Error with no `.mark` property (no line/column in output)

    **validator.ts potential gaps:**
    - `additionalProperties` keyword branch in formatErrorMessage (test with unknown property in spec)
    - `enum` keyword branch in formatErrorMessage (test with invalid enum value)
    - `type` keyword branch in formatErrorMessage (test with wrong type, e.g., string where number expected)
    - Default case in formatErrorMessage
    - Parent path fallback in resolveSourcePositionFromDoc (node not found at exact path)
    - pathToArray with numeric segments

    **expression.ts potential gaps:**
    - `ExpressionError` with position parameter
    - Unterminated string literal error
    - Unexpected character error
    - Number with decimal point (e.g., `3.14`)
    - Null literal
    - MAX_AST_DEPTH exceeded
    - Unknown unary operator branch (defensive, may be unreachable)
    - CallExpression on non-array value error
    - Unknown method error (e.g., `.map()`)
    - `every()` and `some()` with property name argument
    - Short-circuit evaluation for `&&` and `||`
    - Safe navigation returning undefined for null/undefined member access

    **dag.ts potential gaps:**
    - Empty steps input returns `{ ok: true, levels: [], order: [] }`
    - Self-reference cycle detection
    - Missing dependency error
    - Unreachable step detection (step with needs but no path from root)

    4. After adding tests, run `npm test -- --coverage` and verify all four modules show 90%+.
    5. If any module is still below 90%, read the detailed coverage output and add more tests until the threshold is met.
  </action>
  <verify>
    <automated>cd /Users/rainierpotgieter/development/logic-md && npm test -- --coverage 2>&1 | grep -E "(parser|validator|expression|dag)\.ts"</automated>
  </verify>
  <done>All four modules (parser.ts, validator.ts, expression.ts, dag.ts) show 90%+ on lines, branches, functions, and statements in the coverage report. All tests pass.</done>
</task>

</tasks>

<verification>
- `npm test` passes (all existing + new tests)
- `npm test -- --coverage` shows 90%+ on all four modules
- No duplicate tests (each new test covers a previously-uncovered code path)
</verification>

<success_criteria>
parser.ts, validator.ts, expression.ts, and dag.ts each have 90%+ coverage across lines, branches, functions, and statements.
</success_criteria>

<output>
After completion, create `.planning/phases/09-test-coverage-and-integration/09-02-SUMMARY.md`
</output>
