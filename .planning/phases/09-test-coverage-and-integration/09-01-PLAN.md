---
phase: 09-test-coverage-and-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - packages/core/vitest.config.ts
  - .planning/phases/09-test-coverage-and-integration/COVERAGE-BASELINE.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04

must_haves:
  truths:
    - "vitest coverage runs successfully and produces a report"
    - "Coverage baseline for parser, validator, expression, dag is documented"
    - "Gaps below 90% are identified with specific uncovered lines"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Root vitest config with coverage configuration"
      contains: "coverage"
    - path: ".planning/phases/09-test-coverage-and-integration/COVERAGE-BASELINE.md"
      provides: "Baseline coverage numbers and identified gaps"
  key_links:
    - from: "vitest.config.ts"
      to: "@vitest/coverage-v8"
      via: "coverage provider config"
      pattern: "provider.*v8"
---

<objective>
Install @vitest/coverage-v8, configure vitest for coverage reporting, run baseline coverage on the four core modules (parser, validator, expression, dag), and document which modules need additional tests to reach 90%.

Purpose: Establish coverage tooling and identify exact gaps before writing any new tests -- avoid duplicating the 136+ existing tests.
Output: Working coverage command, baseline report documenting per-module coverage and specific uncovered lines/branches.
</objective>

<execution_context>
@/Users/rainierpotgieter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainierpotgieter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@vitest.config.ts
@packages/core/vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install coverage tooling and configure vitest</name>
  <files>vitest.config.ts, packages/core/vitest.config.ts</files>
  <action>
    1. Install @vitest/coverage-v8 as a root dev dependency: `npm install -D @vitest/coverage-v8`
    2. Update the root `vitest.config.ts` to add coverage configuration:
       - provider: "v8"
       - include: ["packages/core/parser.ts", "packages/core/validator.ts", "packages/core/expression.ts", "packages/core/dag.ts"]
       - reporter: ["text", "json-summary"]
       - thresholds: { lines: 90, functions: 90, branches: 90, statements: 90 }
    3. Update `packages/core/vitest.config.ts` to add the same coverage config (since it uses project-level config).
       The coverage `include` patterns should be relative to the core package: ["parser.ts", "validator.ts", "expression.ts", "dag.ts"]
    4. Verify `npm test -- --coverage` runs without errors from the repo root.
  </action>
  <verify>
    <automated>cd /Users/rainierpotgieter/development/logic-md && npm test -- --coverage 2>&1 | tail -20</automated>
  </verify>
  <done>Coverage report prints to terminal showing per-file line/branch/function/statement percentages for parser.ts, validator.ts, expression.ts, dag.ts</done>
</task>

<task type="auto">
  <name>Task 2: Run baseline coverage and document gaps</name>
  <files>.planning/phases/09-test-coverage-and-integration/COVERAGE-BASELINE.md</files>
  <action>
    1. Run `npm test -- --coverage` and capture the output.
    2. Create COVERAGE-BASELINE.md documenting:
       - Per-module coverage percentages (lines, branches, functions, statements) for parser.ts, validator.ts, expression.ts, dag.ts
       - Which modules are already at 90%+ (no new tests needed)
       - For modules below 90%, list the specific uncovered lines and branches from the coverage report
       - A prioritized list of what tests to write in Plan 02
    3. If ALL four modules are already at 90%+, note that Plan 02 can focus solely on edge cases and integration tests.
    4. Include the imports.ts coverage as well (needed for integration tests in Plan 03).
  </action>
  <verify>
    <automated>test -f /Users/rainierpotgieter/development/logic-md/.planning/phases/09-test-coverage-and-integration/COVERAGE-BASELINE.md && echo "BASELINE EXISTS"</automated>
  </verify>
  <done>COVERAGE-BASELINE.md exists with per-module percentages and a clear gap analysis identifying exactly what needs testing</done>
</task>

</tasks>

<verification>
- `npm test -- --coverage` completes successfully
- Coverage report shows all four target modules
- COVERAGE-BASELINE.md contains specific per-module numbers
</verification>

<success_criteria>
Coverage tooling installed and configured. Baseline coverage documented with specific gaps identified for each module below 90%.
</success_criteria>

<output>
After completion, create `.planning/phases/09-test-coverage-and-integration/09-01-SUMMARY.md`
</output>
