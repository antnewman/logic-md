---
phase: 09-test-coverage-and-integration
plan: 03
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - packages/core/integration.test.ts
autonomous: true
requirements:
  - TEST-05

must_haves:
  truths:
    - "Integration test parses a real LOGIC.md file through the full pipeline"
    - "Integration test validates parsed spec against schema"
    - "Integration test resolves DAG from validated spec"
    - "Integration test resolves imports with namespace merging"
    - "Edge cases are tested: malformed files, circular deps, invalid expressions"
  artifacts:
    - path: "packages/core/integration.test.ts"
      provides: "End-to-end pipeline integration tests"
      min_lines: 80
  key_links:
    - from: "packages/core/integration.test.ts"
      to: "packages/core/parser.ts"
      via: "import { parse }"
      pattern: "import.*parse"
    - from: "packages/core/integration.test.ts"
      to: "packages/core/validator.ts"
      via: "import { validate }"
      pattern: "import.*validate"
    - from: "packages/core/integration.test.ts"
      to: "packages/core/dag.ts"
      via: "import { resolve }"
      pattern: "import.*resolve"
    - from: "packages/core/integration.test.ts"
      to: "packages/core/imports.ts"
      via: "import { resolveImports }"
      pattern: "import.*resolveImports"
    - from: "packages/core/integration.test.ts"
      to: "packages/core/expression.ts"
      via: "import { evaluate }"
      pattern: "import.*evaluate"
---

<objective>
Create end-to-end integration tests that prove the full logic-md pipeline works: parse -> validate -> resolve DAG -> resolve imports -> evaluate expressions. Test both happy paths and error paths.

Purpose: Prove the modules compose correctly as a pipeline, catching integration bugs that unit tests miss.
Output: integration.test.ts with full pipeline tests covering success, error, and edge case scenarios.
</objective>

<execution_context>
@/Users/rainierpotgieter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainierpotgieter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-test-coverage-and-integration/09-01-SUMMARY.md
@packages/core/parser.ts
@packages/core/validator.ts
@packages/core/expression.ts
@packages/core/dag.ts
@packages/core/imports.ts
@packages/core/types.ts
@packages/core/__fixtures__
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create full pipeline integration tests</name>
  <files>packages/core/integration.test.ts</files>
  <action>
    1. Create `packages/core/integration.test.ts` with imports from all five modules: parse, validate, resolve (dag), resolveImports, evaluate.
    2. Write test groups:

    **"full pipeline: valid spec"** (happy path):
    - Create a realistic LOGIC.md string with frontmatter containing: spec_version, name, description, reasoning strategy with steps that have `needs` dependencies, gates, and expressions.
    - Pipeline: parse(content) -> assert ok -> validate(content) -> assert ok -> resolve(parsed.data.steps) -> assert ok with correct topological order and parallel levels.
    - Evaluate gate expressions from the spec against mock context and assert correct results.

    **"full pipeline: multi-step DAG"**:
    - Create a spec with 4+ steps, some parallel, some sequential via `needs`.
    - Parse -> validate -> resolve -> verify levels array groups independent steps together.

    **"pipeline error: malformed YAML"**:
    - Content with broken YAML (e.g., unclosed string, bad indentation).
    - parse() returns ok: false with descriptive error. Pipeline stops here.

    **"pipeline error: schema validation failure"**:
    - Content with valid YAML but missing required fields (e.g., no `spec_version`).
    - parse() succeeds, validate() returns ok: false with error messages and line numbers.

    **"pipeline error: circular dependencies"**:
    - Valid spec with steps A needs B, B needs A.
    - Parse and validate succeed, resolve() returns ok: false with cycle error.

    **"pipeline error: invalid expression"**:
    - evaluate() with malformed expression (e.g., missing closing delimiter, unknown operator).
    - Assert ExpressionError is thrown with descriptive message.

    **"pipeline: import resolution"** (uses fixture files):
    - Check if __fixtures__ has import test files. If not, create inline fixture files using `writeFileSync` to a tmp directory, then clean up.
    - Create a base spec with `imports: [{ ref: "./child.logic.md", as: "child" }]`.
    - Write child spec to tmp dir.
    - resolveImports(parsedSpec, tmpDir) -> assert ok, verify namespaced steps appear as `child.stepName`.

    **"pipeline: circular import detection"**:
    - Two specs that import each other.
    - resolveImports returns ok: false with circular_import error.

    3. All tests must be self-contained (no external file dependencies except tmp files cleaned up in afterEach).
    4. Run `npm test` to verify all integration tests pass.
  </action>
  <verify>
    <automated>cd /Users/rainierpotgieter/development/logic-md && npx vitest run packages/core/integration.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>integration.test.ts contains 8+ test cases covering the full pipeline (parse -> validate -> resolve DAG -> resolve imports -> evaluate). All tests pass. Both happy paths and error paths (malformed YAML, schema errors, circular deps, invalid expressions, circular imports) are covered.</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/core/integration.test.ts` passes all tests
- Tests cover the full pipeline: parse -> validate -> resolve -> imports -> evaluate
- Error paths tested: malformed files, circular deps, invalid expressions
- No hardcoded file paths (uses tmp directories or inline strings)
</verification>

<success_criteria>
Integration tests prove the full logic-md pipeline works end-to-end. Both success and error scenarios are covered. All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/09-test-coverage-and-integration/09-03-SUMMARY.md`
</output>
