---
phase: 07-import-resolver
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - packages/core/index.ts
  - packages/core/imports.test.ts
autonomous: true
requirements:
  - IMPT-01
  - IMPT-02
  - IMPT-03
  - IMPT-04

must_haves:
  truths:
    - "resolveImports is importable from @logic-md/core barrel export"
    - "All import result types are re-exported from barrel"
    - "Edge cases do not crash the resolver"
  artifacts:
    - path: "packages/core/index.ts"
      provides: "Barrel re-export of resolveImports and import types"
      contains: "resolveImports"
    - path: "packages/core/imports.test.ts"
      provides: "Extended test suite with edge case coverage"
      min_lines: 120
  key_links:
    - from: "packages/core/index.ts"
      to: "packages/core/imports.ts"
      via: "re-export statement"
      pattern: "from.*imports"
---

<objective>
Wire resolveImports into the barrel export and add edge case tests.

Purpose: Make the import resolver accessible from the public API and harden it against edge cases identified in research.

Output: Updated barrel export, extended test coverage.
</objective>

<execution_context>
@/Users/rainierpotgieter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainierpotgieter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-import-resolver/07-01-SUMMARY.md
@packages/core/index.ts
@packages/core/imports.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Barrel export wiring and edge case tests</name>
  <files>
    packages/core/index.ts
    packages/core/imports.test.ts
  </files>
  <action>
    **Barrel export (index.ts):**

    Add re-exports from `./imports.js` to `packages/core/index.ts`, following the exact pattern of existing exports (alphabetical placement between expression and parser):

    ```
    export {
      type ImportError,
      type ImportFailure,
      type ImportResult,
      type ImportSuccess,
      resolveImports,
    } from "./imports.js";
    ```

    **Edge case tests (imports.test.ts):**

    Add these additional test cases to the existing test file:

    1. "handles import with invalid YAML in referenced file" -- create a temp fixture (or inline) with broken YAML content. Assert ok: false, error type "parse_error".

    2. "detects duplicate as namespaces" -- parse a spec with `imports: [{ ref: "./a.md", as: "x" }, { ref: "./b.md", as: "x" }]`. Assert ok: false, error type "merge_error", message mentions duplicate namespace.

    3. "resolves paths with .. traversals correctly" -- create a fixture that uses `../` in the ref path. Verify it resolves correctly (or returns appropriate error if file doesn't exist). This tests path normalization.

    4. "namespaces decision_trees keys" -- create a fixture with a `decision_trees` section. Import it and verify keys are prefixed with namespace.

    5. "preserves spec_version and name from local spec" -- after resolving imports, verify the merged result has the local file's spec_version and name (not the imported file's).

    For test fixtures that need temp files, either create them in __fixtures__/ or use inline `parse()` calls + `fs.mkdtempSync`/`fs.writeFileSync` for truly temp files. Use `afterAll` or `afterEach` to clean up temp files if created outside __fixtures__/.

    Run the full test suite to verify everything passes.
  </action>
  <verify>
    <automated>cd /Users/rainierpotgieter/development/logic-md && npx vitest run packages/core/imports.test.ts && npx vitest run packages/core/index.test.ts && npx tsc --noEmit -p packages/core/tsconfig.json</automated>
    <manual>All tests pass including new edge cases. Barrel export compiles cleanly.</manual>
  </verify>
  <done>resolveImports and all Import* types are importable from @logic-md/core. Edge case tests pass for: invalid YAML imports, duplicate namespaces, path traversals, decision_tree namespacing, local spec_version/name preservation. Full typecheck passes.</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/core/` -- all core tests pass (imports + existing modules)
- `npx tsc --noEmit -p packages/core/tsconfig.json` -- no type errors
- `import { resolveImports } from "@logic-md/core"` works
</verification>

<success_criteria>
- resolveImports importable from barrel export
- 12+ total tests covering happy paths and edge cases
- All existing tests still pass (no regressions)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-import-resolver/07-02-SUMMARY.md`
</output>
