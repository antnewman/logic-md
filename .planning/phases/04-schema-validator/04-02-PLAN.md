---
phase: 04-schema-validator
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - packages/core/validator.ts
  - packages/core/validator.test.ts
  - packages/core/index.ts
autonomous: true
requirements:
  - PARS-04

must_haves:
  truths:
    - "Each validation error includes a line number pointing to the correct location in the source file"
    - "Line numbers account for the opening --- delimiter (offset by +1)"
    - "validate() and ValidationResult types are exported from @logic-md/core barrel"
  artifacts:
    - path: "packages/core/validator.test.ts"
      provides: "Tests verifying line number accuracy"
      contains: "line"
    - path: "packages/core/index.ts"
      provides: "Barrel exports for validate and validation types"
      contains: "validate"
  key_links:
    - from: "packages/core/validator.ts"
      to: "yaml"
      via: "LineCounter + parseDocument for source positions"
      pattern: "lineCounter.linePos"
    - from: "packages/core/index.ts"
      to: "packages/core/validator.ts"
      via: "barrel re-export"
      pattern: "from.*validator"
---

<objective>
Add line number accuracy tests and wire up barrel exports for the validator module.

Purpose: PARS-04 requires errors include line numbers. Plan 01 builds the mechanism; this plan adds targeted tests proving line numbers are correct, handles edge cases (array paths, deep nesting), and exports everything from the barrel.

Output: Line-number-verified validator, complete barrel exports from @logic-md/core.
</objective>

<execution_context>
@/Users/rainierpotgieter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainierpotgieter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-schema-validator/04-RESEARCH.md
@.planning/phases/04-schema-validator/04-01-SUMMARY.md
@packages/core/validator.ts
@packages/core/validator.test.ts
@packages/core/index.ts
@packages/core/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add line number accuracy tests and edge case coverage</name>
  <files>packages/core/validator.test.ts</files>
  <action>
Add a new `describe("line numbers")` block to the existing validator.test.ts with these test cases:

1. **Line number for invalid nested property**: Create a LOGIC.md string where `reasoning.strategy` has an invalid value (e.g., `strategy: "invalid-strategy"`). The YAML should be multi-line so line numbers are meaningful. Assert the error has a `line` property that is a number greater than 1 (the exact line where `strategy` appears in the source, accounting for `---` offset).

2. **Line number offset for frontmatter delimiter**: Create a simple LOGIC.md string:
   ```
   ---
   spec_version: "1.0"
   foo: bar
   ---
   ```
   The `foo` property is on line 3 of the file. Assert the `additionalProperties` error for `foo` has `line: 3`.

3. **Multiple errors have distinct line numbers**: Create a LOGIC.md string with errors on different lines (e.g., unknown properties on lines 3 and 4). Assert that the returned errors have different `line` values.

4. **Deep path line numbers**: Create a LOGIC.md with a `steps` section containing a step with an invalid property. Assert the error line points into the steps section, not the document root.

Run tests after adding:
```bash
cd /Users/rainierpotgieter/development/logic-md && npx vitest run packages/core/validator.test.ts
```

If any line number tests fail, fix the source position mapping in validator.ts (adjust FRONTMATTER_OFFSET, path resolution, or node lookup). The tests are the source of truth.
  </action>
  <verify>
    <automated>cd /Users/rainierpotgieter/development/logic-md && npx vitest run packages/core/validator.test.ts</automated>
  </verify>
  <done>All line number tests pass, confirming errors include correct 1-indexed file line numbers with frontmatter offset applied.</done>
</task>

<task type="auto">
  <name>Task 2: Update barrel exports and run full test suite</name>
  <files>packages/core/index.ts</files>
  <action>
1. Update `packages/core/index.ts` to export the validator function and types. Add these lines:

   ```typescript
   export { validate } from "./validator.js";
   export type {
     ValidationError,
     ValidationFailure,
     ValidationResult,
     ValidationSuccess,
   } from "./types.js";
   ```

   Keep all existing exports intact. Place the new exports after existing ones.

2. Run the full test suite to confirm nothing is broken:
   ```bash
   cd /Users/rainierpotgieter/development/logic-md && npm test
   ```

3. Run typecheck:
   ```bash
   cd /Users/rainierpotgieter/development/logic-md && npx tsc --noEmit -p packages/core/tsconfig.json
   ```

4. Run lint:
   ```bash
   cd /Users/rainierpotgieter/development/logic-md && npm run lint
   ```
  </action>
  <verify>
    <automated>cd /Users/rainierpotgieter/development/logic-md && npm test && npx tsc --noEmit -p packages/core/tsconfig.json && npm run lint</automated>
  </verify>
  <done>validate and all ValidationResult types exported from @logic-md/core. Full test suite passes. Typecheck and lint clean.</done>
</task>

</tasks>

<verification>
- `npm test` -- all tests pass (existing + new validator tests)
- `npx tsc --noEmit -p packages/core/tsconfig.json` -- clean
- `npm run lint` -- clean
- Line number tests specifically verify PARS-04 (errors include line numbers)
</verification>

<success_criteria>
- Validation errors include accurate line numbers pointing to the source YAML location
- Line numbers account for the `---` frontmatter delimiter offset
- validate() and all validation types are importable from @logic-md/core
- Full test suite, typecheck, and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-schema-validator/04-02-SUMMARY.md`
</output>
