---
phase: 17-compiler-test-coverage
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/compiler.test.ts
autonomous: true
requirements:
  - CTST-01
  - CTST-02
  - CTST-03

must_haves:
  truths:
    - "vitest coverage report shows 90%+ on compiler.ts for lines, branches, functions, statements"
    - "Tests explicitly cover linear workflow (A->B->C chain)"
    - "Tests explicitly cover branching workflow (step with branches compiled in workflow)"
    - "Tests explicitly cover parallel step groups at same DAG level"
    - "Tests cover unsupported self-reflection strategy returning null (line 248)"
    - "Tests cover token estimation warning threshold (2000 tokens)"
    - "Tests cover edge cases: reflection with default prompt, rubric with empty criteria"
  artifacts:
    - path: "packages/core/compiler.test.ts"
      provides: "Comprehensive compiler test suite with gap-filling tests"
      contains: "unsupported strategy"
  key_links:
    - from: "packages/core/compiler.test.ts"
      to: "packages/core/compiler.ts"
      via: "import { compileStep, compileWorkflow, estimateTokens }"
      pattern: "import.*compiler"
---

<objective>
Audit existing compiler tests and add targeted tests to close coverage gaps and satisfy CTST-01/02/03 requirements.

Purpose: The compiler already has 99%+ statement coverage and 98 passing tests. This plan adds the remaining scenario tests required by the requirements (explicit linear/branching/parallel workflow tests, unsupported strategy branch, edge cases) and closes the single uncovered branch (line 248).

Output: Updated compiler.test.ts with all required scenario coverage, 90%+ across all coverage dimensions confirmed.
</objective>

<execution_context>
@/Users/rainierpotgieter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainierpotgieter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/compiler.ts
@packages/core/compiler.test.ts
@packages/core/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing scenario tests for workflow shapes and self-reflection edge cases</name>
  <files>packages/core/compiler.test.ts</files>
  <action>
Add the following test groups to the END of compiler.test.ts (after the existing compileWorkflow describe block):

1. **"compileWorkflow: linear workflow"** describe block:
   - Create a spec with 3 steps: `step_a` (no deps), `step_b` (needs: [step_a]), `step_c` (needs: [step_b])
   - Test that compileWorkflow returns 3 DAG levels, each with exactly 1 step
   - Test that steps are ordered [step_a, step_b, step_c]
   - Test metadata.totalLevels === 3

2. **"compileWorkflow: branching workflow"** describe block:
   - Create a spec with steps: `analyze` (has branches: [{if: "{{ score > 0.8 }}", then: "accept"}, {default: true, then: "revise"}]), `accept` (needs: [analyze]), `revise` (needs: [analyze])
   - Test that compileWorkflow produces correct DAG levels (analyze at level 0, accept+revise at level 1)
   - Test that the compiled step for `analyze` step contains the branches metadata

3. **"compileWorkflow: parallel step groups"** describe block:
   - Create a spec with: `start` (no deps), `parallel_a` (needs: [start]), `parallel_b` (needs: [start]), `merge` (needs: [parallel_a, parallel_b])
   - Test dagLevels[1] contains both parallel_a and parallel_b (length 2)
   - Test dagLevels[2] contains only merge
   - Test metadata.totalLevels === 3

4. **"compileSelfReflection: unsupported strategy"** describe block:
   - Create a spec with self_verification: { enabled: true, strategy: "checklist", checklist: ["item1"] }
   - Test that compileStep returns selfReflection === null (covers line 248)
   - Also test with strategy: "critic" returning null

5. **"compileSelfReflection: edge cases"** describe block:
   - Test reflection strategy with NO explicit prompt (should use default "Review your output for accuracy and completeness.")
   - Test rubric with empty criteria array (criteria: []) — should still compile with minimumScore
   - Test rubric with no criteria field at all (rubric: { minimum_score: 0.8 }) — should compile

6. **"compileWorkflow: token warning propagation"** describe block:
   - Create a spec with one step that has very long instructions (10000+ chars)
   - Compile workflow
   - Verify the compiled step within the workflow has tokenWarning defined

7. **"compileWorkflow: missing optional fields"** describe block:
   - Create a spec with steps that have no description, no instructions, no retry, no confidence, no verification, no output_schema — just empty step objects
   - Test compileWorkflow succeeds without error
   - Test compiled steps have null retryPolicy, null selfReflection, empty qualityGates

Use the existing helper functions (makeSpec, makeCtx, makeWorkflowCtx) throughout. Do NOT modify any existing tests.
  </action>
  <verify>
    <automated>cd /Users/rainierpotgieter/development/logic-md && npx vitest run packages/core/compiler.test.ts 2>&1 | tail -20</automated>
    <manual>All existing 98 tests still pass, new tests also pass</manual>
  </verify>
  <done>All new scenario tests pass alongside existing 98 tests. Tests explicitly named for linear workflows, branching workflows, parallel step groups, unsupported self-reflection strategies, and edge cases.</done>
</task>

<task type="auto">
  <name>Task 2: Verify 90%+ coverage across all dimensions</name>
  <files>packages/core/compiler.test.ts</files>
  <action>
Run vitest with coverage scoped to compiler.ts:

```
npx vitest run packages/core/compiler.test.ts --coverage --coverage.include='packages/core/compiler.ts'
```

Verify all four dimensions are 90%+:
- Statements: 90%+
- Branches: 90%+ (was 93.44%, should now be higher with line 248 covered)
- Functions: 90%+ (was 100%)
- Lines: 90%+ (was 99.22%)

If any dimension is below 90%, identify the uncovered lines/branches from the coverage report and add targeted tests. The unsupported strategy test from Task 1 should have closed the only known branch gap (line 248).

No changes needed to compiler.ts — this is test-only work.
  </action>
  <verify>
    <automated>cd /Users/rainierpotgieter/development/logic-md && npx vitest run packages/core/compiler.test.ts --coverage --coverage.include='packages/core/compiler.ts' 2>&1 | grep -E "compiler\.ts|All files"</automated>
    <manual>Coverage report shows 90%+ on all four dimensions for compiler.ts</manual>
  </verify>
  <done>vitest coverage report confirms 90%+ statements, branches, functions, and lines on compiler.ts. CTST-01 requirement satisfied.</done>
</task>

</tasks>

<verification>
1. `npx vitest run packages/core/compiler.test.ts` — all tests pass (existing + new)
2. `npx vitest run packages/core/compiler.test.ts --coverage --coverage.include='packages/core/compiler.ts'` — 90%+ on all dimensions
3. Test names explicitly reference: "linear workflow", "branching workflow", "parallel step groups", "unsupported strategy", "token warning", "missing optional fields"
</verification>

<success_criteria>
1. All compiler tests pass (100+ tests total)
2. Coverage report shows 90%+ on statements, branches, functions, and lines for compiler.ts
3. Tests explicitly named for every scenario in CTST-02 (linear, branching, retry context, quality gates) and CTST-03 (self-reflection, parallel groups, token warnings, edge cases)
4. Line 248 branch (unsupported strategy) now covered
</success_criteria>

<output>
After completion, create `.planning/phases/17-compiler-test-coverage/17-01-SUMMARY.md`
</output>
