---
phase: 12-step-compiler-context
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/types.ts
  - packages/core/compiler.ts
  - packages/core/compiler.test.ts
autonomous: true
requirements: [COMP-04, COMP-05]

must_haves:
  truths:
    - "When branchReason is set, systemPromptSegment contains a Branch Context section explaining why this branch was taken"
    - "When branchReason is set and step has branches, alternatives are listed in the prompt"
    - "When attemptNumber > 1, systemPromptSegment contains a Retry Context section with attempt number"
    - "When previousFailureReason is set, retry context includes the failure reason"
    - "When branchReason is null and attemptNumber is 1, no branch/retry sections appear"
  artifacts:
    - path: "packages/core/types.ts"
      provides: "ExecutionContext with previousFailureReason field"
      contains: "previousFailureReason"
    - path: "packages/core/compiler.ts"
      provides: "formatBranchContext and formatRetryContext helpers"
    - path: "packages/core/compiler.test.ts"
      provides: "Tests for branch context and retry context in systemPromptSegment"
  key_links:
    - from: "packages/core/compiler.ts"
      to: "packages/core/types.ts"
      via: "ExecutionContext.previousFailureReason"
      pattern: "context\\.previousFailureReason"
    - from: "packages/core/compiler.ts"
      to: "packages/core/types.ts"
      via: "Step.branches for alternative listing"
      pattern: "step\\.branches"
---

<objective>
Add branch context and retry context sections to compileStep's systemPromptSegment.

Purpose: When a step is reached via a decision branch or is being retried, the compiled prompt must explain this context to the LLM so it understands why it's executing and what went wrong before.

Output: Updated compiler with two new prompt sections, type addition for previousFailureReason, and TDD tests.
</objective>

<execution_context>
@/Users/rainierpotgieter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainierpotgieter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-step-compiler-core/11-01-SUMMARY.md
@.planning/phases/11-step-compiler-core/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED -- Write failing tests for branch context and retry context</name>
  <files>packages/core/types.ts, packages/core/compiler.test.ts</files>
  <action>
1. Add `previousFailureReason: string | null` field to the `ExecutionContext` interface in types.ts (after `branchReason`).

2. Update the `makeCtx` helper in compiler.test.ts to include `previousFailureReason: null` in the default context.

3. Add a new describe block `"systemPromptSegment: branch context"` with these tests:

- "includes branch context section when branchReason is set": compile with `branchReason: "confidence below threshold"`, assert systemPromptSegment contains `"## Branch Context"` and the branch reason string.

- "explains alternatives when step has branches": Create a spec where the step being compiled has `branches: [{ if: '{{ confidence > 0.8 }}', then: 'step_a' }, { default: true, then: 'step_b' }]`. Compile with `branchReason: "confidence check"`. Assert the prompt contains the word "alternatives" or "Alternative" and mentions `step_a` and `step_b`.

- "omits branch context when branchReason is null": compile with default context (branchReason: null), assert systemPromptSegment does NOT contain `"## Branch Context"`.

4. Add a new describe block `"systemPromptSegment: retry context"` with these tests:

- "includes retry context section when attemptNumber > 1": compile with `attemptNumber: 2`, assert systemPromptSegment contains `"## Retry Context"` and `"Attempt 2"`.

- "includes previous failure reason when set": compile with `attemptNumber: 3, previousFailureReason: "Output schema validation failed"`, assert systemPromptSegment contains `"Output schema validation failed"`.

- "includes max attempts from retry policy when step has retry config": compile gather_sources from researchSpec with `attemptNumber: 2`, assert prompt contains `"3"` (max_attempts from the retry config).

- "omits retry context when attemptNumber is 1": compile with default context (attemptNumber: 1), assert systemPromptSegment does NOT contain `"## Retry Context"`.

- "omits failure reason line when previousFailureReason is null": compile with `attemptNumber: 2, previousFailureReason: null`, assert prompt contains `"## Retry Context"` but does NOT contain `"Previous failure"` or `"failure reason"` (case-insensitive check not needed, just check for the label text you will use).

Run tests: `cd /Users/rainierpotgieter/development/logic-md && npx vitest run packages/core/compiler.test.ts`
New tests MUST fail (RED). Existing tests must still pass (the makeCtx update must be backward-compatible).
  </action>
  <verify>
    <automated>cd /Users/rainierpotgieter/development/logic-md && npx vitest run packages/core/compiler.test.ts 2>&1 | tail -5</automated>
    <manual>New tests fail, existing tests pass. Total failures should be exactly the count of new test cases.</manual>
  </verify>
  <done>New tests written and failing. ExecutionContext type updated with previousFailureReason. Existing 55 tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN -- Implement branch context and retry context formatters</name>
  <files>packages/core/compiler.ts</files>
  <action>
1. Add a `formatBranchContext` internal helper function:
   - Signature: `formatBranchContext(reason: string, step: Step): string`
   - Output a `## Branch Context` heading
   - Next line: `"This step was reached because: {reason}"`
   - If `step.branches` exists and has entries, add a blank line then `"Alternative branches from this step:"` followed by a list of each branch: `"- {then}" (condition: {if})` or `"- {then} (default)"` for default branches.
   - Return joined lines.

2. Add a `formatRetryContext` internal helper function:
   - Signature: `formatRetryContext(attemptNumber: number, previousFailureReason: string | null, retryConfig: RetryConfig | undefined): string`
   - Output a `## Retry Context` heading
   - Next line: `"Attempt {attemptNumber}"` -- if retryConfig exists, append `" of {retryConfig.max_attempts ?? 3}"` to the same line.
   - If `previousFailureReason` is not null, add: `"Previous failure: {previousFailureReason}"`
   - Return joined lines.

3. Wire into `compileStep` -- after the step instructions segment and before the output schema segment:
   - If `context.branchReason` is not null, push `formatBranchContext(context.branchReason, step)` into segments.
   - If `context.attemptNumber > 1`, push `formatRetryContext(context.attemptNumber, context.previousFailureReason, step.retry)` into segments.

4. Update the `compileStep` function signature area: the `context` parameter already typed as `ExecutionContext` which now includes `previousFailureReason` from Task 1.

Run all tests: `cd /Users/rainierpotgieter/development/logic-md && npx vitest run packages/core/compiler.test.ts`
ALL tests must pass (GREEN).
  </action>
  <verify>
    <automated>cd /Users/rainierpotgieter/development/logic-md && npx vitest run packages/core/compiler.test.ts</automated>
    <manual>All tests pass including the new branch/retry context tests.</manual>
  </verify>
  <done>All tests pass. Branch context appears when branchReason is set. Retry context appears when attemptNumber > 1. Neither section appears in default context.</done>
</task>

</tasks>

<verification>
cd /Users/rainierpotgieter/development/logic-md && npx vitest run packages/core/compiler.test.ts
All tests pass (55 existing + ~8 new = ~63 total).
</verification>

<success_criteria>
- compileStep with branchReason set produces "## Branch Context" section in systemPromptSegment
- compileStep with branches on the step lists alternatives
- compileStep with attemptNumber > 1 produces "## Retry Context" section
- compileStep with previousFailureReason includes failure description
- Default context (branchReason: null, attemptNumber: 1) produces no branch/retry sections
- All existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/12-step-compiler-context/12-01-SUMMARY.md`
</output>
