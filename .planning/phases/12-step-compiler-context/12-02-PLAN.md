---
phase: 12-step-compiler-context
plan: 02
type: tdd
wave: 2
depends_on: [12-01]
files_modified:
  - packages/core/compiler.ts
  - packages/core/compiler.test.ts
autonomous: true
requirements: [COMP-07, COMP-08]

must_haves:
  truths:
    - "When a step has confidence thresholds, systemPromptSegment contains a Confidence Requirements section"
    - "Confidence section references minimum and target values from step.confidence"
    - "When a step has verification checks, systemPromptSegment contains a quality gate checklist"
    - "When spec has global quality_gates, systemPromptSegment includes pre_output gate descriptions"
    - "Steps without confidence or quality gates produce no additional sections"
  artifacts:
    - path: "packages/core/compiler.ts"
      provides: "formatConfidenceRequirements and formatQualityGateChecklist helpers"
    - path: "packages/core/compiler.test.ts"
      provides: "Tests for confidence and quality gate sections in systemPromptSegment"
  key_links:
    - from: "packages/core/compiler.ts"
      to: "packages/core/types.ts"
      via: "Step.confidence (ConfidenceConfig)"
      pattern: "step\\.confidence"
    - from: "packages/core/compiler.ts"
      to: "packages/core/types.ts"
      via: "Step.verification and spec.quality_gates"
      pattern: "(step\\.verification|spec\\.quality_gates)"
---

<objective>
Add confidence threshold instructions and quality gate checklists to compileStep's systemPromptSegment.

Purpose: Steps with confidence requirements need to instruct the LLM to meet minimum confidence levels. Steps with quality gates need a "Before responding, verify: ..." checklist so the LLM self-checks before producing output.

Output: Updated compiler with two new prompt sections and TDD tests.
</objective>

<execution_context>
@/Users/rainierpotgieter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainierpotgieter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-step-compiler-context/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED -- Write failing tests for confidence and quality gate sections</name>
  <files>packages/core/compiler.test.ts</files>
  <action>
1. Add a new describe block `"systemPromptSegment: confidence requirements"` with these tests:

- "includes confidence section when step has confidence config": Create a spec with a step that has `confidence: { minimum: 0.7, target: 0.9 }`. Compile it. Assert systemPromptSegment contains `"## Confidence Requirements"`.

- "shows minimum confidence threshold": Same spec. Assert prompt contains `"0.7"` and text like `"minimum"` (case-insensitive match via toContain on the exact string you'll use).

- "shows target confidence when set": Same spec. Assert prompt contains `"0.9"` and `"target"`.

- "shows escalate_below when set": Create a spec with `confidence: { minimum: 0.5, escalate_below: 0.3 }`. Assert prompt contains `"0.3"` and text about escalation.

- "omits confidence section when step has no confidence config": Use researchSpec's gather_sources (no confidence). Assert systemPromptSegment does NOT contain `"## Confidence Requirements"`.

2. Add a new describe block `"systemPromptSegment: quality gate checklist"` with these tests:

- "includes quality gate checklist from step.verification": Create a spec with a step that has `verification: { check: '{{ output.sources.length >= 3 }}', on_fail: 'retry', on_fail_message: 'Need at least 3 sources' }`. Compile it. Assert systemPromptSegment contains `"## Pre-Response Checklist"` (or `"Before responding, verify"`) and `"Need at least 3 sources"`.

- "includes pre_output gates from spec.quality_gates": Create a spec with `quality_gates: { pre_output: [{ name: 'length_check', check: '{{ output.length > 100 }}', message: 'Response must be over 100 chars' }] }`. Compile any step. Assert systemPromptSegment contains `"Response must be over 100 chars"`.

- "combines step verification and global quality gates": Create a spec with BOTH step.verification AND spec.quality_gates.pre_output. Assert both messages appear in the checklist.

- "omits checklist when no verification and no quality_gates": Use a bare step spec. Assert systemPromptSegment does NOT contain `"## Pre-Response Checklist"`.

- "uses gate name as fallback when message is missing": Create a spec with `quality_gates: { pre_output: [{ name: 'format_check', check: '{{ valid }}' }] }` (no message field). Assert prompt contains `"format_check"`.

Run tests: `cd /Users/rainierpotgieter/development/logic-md && npx vitest run packages/core/compiler.test.ts`
New tests MUST fail (RED). Existing tests must still pass.
  </action>
  <verify>
    <automated>cd /Users/rainierpotgieter/development/logic-md && npx vitest run packages/core/compiler.test.ts 2>&1 | tail -5</automated>
    <manual>New tests fail, existing tests (including Plan 01's tests) pass.</manual>
  </verify>
  <done>New tests written and failing. Existing tests pass including branch/retry context tests from Plan 01.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN -- Implement confidence and quality gate formatters</name>
  <files>packages/core/compiler.ts</files>
  <action>
1. Add a `formatConfidenceRequirements` internal helper:
   - Signature: `formatConfidenceRequirements(confidence: ConfidenceConfig): string`
   - Import `ConfidenceConfig` from types.
   - Output a `## Confidence Requirements` heading.
   - If `confidence.minimum` is set: `"You must achieve a minimum confidence of {minimum} in your response."`
   - If `confidence.target` is set: `"Target confidence level: {target}"`
   - If `confidence.escalate_below` is set: `"If your confidence falls below {escalate_below}, escalate to a human reviewer rather than proceeding."`
   - Return joined lines.

2. Add a `formatQualityGateChecklist` internal helper:
   - Signature: `formatQualityGateChecklist(step: Step, spec: LogicSpec): string`
   - Collect checklist items from two sources:
     a. If `step.verification` exists, add an item: `step.verification.on_fail_message` or fall back to `step.verification.check`.
     b. If `spec.quality_gates?.pre_output` exists, add an item for each gate: `gate.message` or fall back to `gate.name`.
   - If no items collected, return empty string.
   - Output: `## Pre-Response Checklist` heading, then `"Before responding, verify:"`, then each item as `"- [ ] {item}"`.
   - Return joined lines.

3. Wire into `compileStep` -- after branch/retry context, before output schema:
   - If `step.confidence` exists, push `formatConfidenceRequirements(step.confidence)`.
   - Call `formatQualityGateChecklist(step, spec)` -- if non-empty string returned, push it.

Run all tests: `cd /Users/rainierpotgieter/development/logic-md && npx vitest run packages/core/compiler.test.ts`
ALL tests must pass (GREEN).
  </action>
  <verify>
    <automated>cd /Users/rainierpotgieter/development/logic-md && npx vitest run packages/core/compiler.test.ts</automated>
    <manual>All tests pass including confidence and quality gate tests.</manual>
  </verify>
  <done>All tests pass. Confidence section appears when step.confidence is set. Quality gate checklist appears when step.verification or spec.quality_gates.pre_output exists. Neither section appears for bare steps.</done>
</task>

</tasks>

<verification>
cd /Users/rainierpotgieter/development/logic-md && npx vitest run packages/core/compiler.test.ts
All tests pass (~63 from Plan 01 + ~10 new = ~73 total).
</verification>

<success_criteria>
- compileStep with step.confidence produces "## Confidence Requirements" section with minimum/target/escalate values
- compileStep with step.verification or spec.quality_gates produces "## Pre-Response Checklist" with checklist items
- Default steps without confidence or gates produce no additional sections
- All existing tests still pass (no regressions)
- Prompt section ordering: Strategy > Instructions > Branch > Retry > Confidence > Quality Gates > Output Format
</success_criteria>

<output>
After completion, create `.planning/phases/12-step-compiler-context/12-02-SUMMARY.md`
</output>
